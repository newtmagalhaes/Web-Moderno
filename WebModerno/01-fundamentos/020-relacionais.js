// Operadores relacionais em JavaScript

// Operadores relacionais retornam Booleans
console.log(`operadores bínários:
equivalência:           '1' == 1 : ${"1" == 1};
equivalência estrita:   '1' === 1: ${"1" === 1};
diferença:              '1' != 1 : ${"1" != 1};
diferença estrita:      '1' !== 1: ${"1" !== 1};
menor que:              1 < 0    : ${1 < 0};
maior que:              1 > 0    : ${1 > 0};
maior ou igual:         1 >= 0   : ${1 >= 0};
menor ou igual:         1 <= 0   : ${1 <= 0};`);
/**
 * Atenção à comparação de dados não primitivos
 * pois o que é comparado é o endereço de memória!!!
 */
